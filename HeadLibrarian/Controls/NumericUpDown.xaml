<UserControl x:Class="HeadLibrarian.Controls.NumericUpDown"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 mc:Ignorable="d"
			 x:Name="NumericUpDownControl">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*" />
			<ColumnDefinition Width="Auto" />
		</Grid.ColumnDefinitions>
		
		<TextBox Grid.Row="0"
				 Grid.RowSpan="2"
				 Grid.Column="0"
				 Text="{Binding Value, ElementName=NumericUpDownControl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
				 PreviewTextInput="PreviewValueTextInput"
				 DataObject.Pasting="ValueTextBoxPasting"
				 BorderThickness="1,1,0,1" />
		
		<Button Grid.Row="0"
				Grid.Column="1"
				IsEnabled="{Binding CanIncrease, ElementName=NumericUpDownControl}"
				Command="{Binding IncreaseCommand, ElementName=NumericUpDownControl}" />
		
		<Button Grid.Row="1"
				Grid.Column="1"
				IsEnabled="{Binding CanDecrease, ElementName=NumericUpDownControl}"
				Command="{Binding DecreaseCommand, ElementName=NumericUpDownControl}" />
	</Grid>
</UserControl>
